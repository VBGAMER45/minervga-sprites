
**File Format Summary:**
- CSV-like text format with signed 16-bit integers
- 54 sprites, each 16×24 pixels
- VGA Mode 12 planar format (4 bit planes per row)
- Each row encoded as 4 integers representing the 4 color planes

**The zip contains:**
- 54 individual PNG sprites (scaled 4× for visibility)
- A combined spritesheet
- A sprite index text file

**Notable sprites include:** tools (shovel, pick, drill, lantern, bucket, torch, dynamite, pump), terrain tiles (earth variants, stone, granite, sandstone, volcanic), building elements (roof pieces, doors, brick patterns), environment (sky, clouds, cactus, water), ores (silver, gold, platinum, diamond), and game objects (elevator parts, condom, ring, etc.)

